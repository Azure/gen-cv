{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 8000"
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^1.1.7"
  }
}
